# ~/.claude config backup (snapshot)

Dated snapshot of the irreplaceable, single-home config files from
C:\Users\pavey\.claude that are otherwise NOT under version control.

Contents:
- CLAUDE.md          global PAIDA config
- .CLAUDE.md         .env protection + supplementary rules
- abbreviations.md   personal abbreviations
- _rules.md          durable behavioural rules (greeting v2.0 mirror, SOP-index, NBLM auth self-heal)
- agents/ron.md      user-level Ron sub-agent definition

This is a SNAPSHOT for backup only, not a live sync. The proper fix is a
dedicated private git repo for the whole ~/.claude folder (see task
"Version-control + back up ~/.claude config", 2026.07.23). Refresh this
snapshot when those files change until that repo exists.

Snapshot created: 2026.07.23
