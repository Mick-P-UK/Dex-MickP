{
  "name": "calendar",
  "description": "Google Calendar integration for meeting prep and scheduling context",
  "server": {
    "command": "python",
    "args": ["core/mcp/calendar_server.py"],
    "env": {
      "VAULT_PATH": "${workspaceFolder}"
    }
  },
  "notes": "Uses Google Calendar API. Cross-platform (Windows, macOS, Linux). Requires Google Calendar API credentials - see System/.credentials/GOOGLE_CALENDAR_SETUP.md for setup instructions."
}
