---
title: Baton Handover -- ShareScope Pipeline v2 -- SOP v2.0 Codified, _make_obsidian_uri Fixed, Ron Auth Quirk Resolved, Ron Now Permanent Named Agent, ENQ End-to-End Validation
date: 2026-07-21
time: 14:35 (London)
topic: ShareScope + NotebookLM + Researcher Ron Pipeline (sharescope-nlm-research skill v2.0)
thread: Direct continuation of the 2026.07.20 17:40 handover. Codified yesterday's decisions into SOP v2.0 (the skill file at Cowork\ShareScope-Project-Setup\3-SKILL-sharescope-nlm-research.md), then executed the four open sub-tasks from that handover in one clean pass: fixed _make_obsidian_uri() in the researcher script to emit vault=&file= format; investigated the "Ron sub-agent NBLM CLI auth quirk" and discovered it was a misdiagnosis (real cause is genuine Google session invalidation that the auth cache can't see); defined Ron as a permanent named agent with definition files in both vaults; then ran a full end-to-end validation on ENQ using every new fix in one go -- Cedric-runs-it autorun, corrected 12-month chart, Nina notebook refresh, Ron spawned via subagent_type: "ron" with a minimal 7-line prompt, report saved as v2 with proper frontmatter and working Obsidian link. Every piece of yesterday's work is now proven live.
status: handover
author: Cedric (PAIDA)
generated-by: AI
---

# Baton Handover -- ShareScope Pipeline v2: SOP Codified, Ron Permanent Agent, ENQ End-to-End Validation (2026-07-21 1435)

Topic: ShareScope + NotebookLM + Researcher Ron Pipeline
Project folder: `C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\04-Projects\2026.04.04-ShareScope-Automation`
Research Log: `C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\06-Resources\Research-Log`
Skill file (SOP v2.0): `C:\Vaults\Cowork\ShareScope-Project-Setup\3-SKILL-sharescope-nlm-research.md`
Ron's definition (both vaults):
  - `C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\.claude\agents\ron.md`
  - `C:\Vaults\Mick's-Writing-System\.claude\agents\ron.md`

## Thread character

Direct continuation of the 2026.07.20 17:40 handover, which left four sub-tasks in priority order under a single "Option 3" umbrella: update the SOP, fix the broken `_make_obsidian_uri()`, investigate Ron's sub-agent auth quirk, and define Ron as a permanent named agent. Mick opened this session with the standard pickup phrase, Cedric read LATEST.md, confirmed state, and Mick greenlit them all in the order Cedric proposed.

**SOP first.** Cedric read the existing `3-SKILL-sharescope-nlm-research.md` (v1.0, ~220 lines, still describing the "Nina does analysis via `notebooklm ask`" pattern and "Tell Mick to run..." for the two Python scripts) and rewrote it wholesale as v2.0. The new SOP is ~330 lines and codifies every decision from the 2026.07.20 session: three-actor architecture (Cedric orchestrates, Nina fetches, Ron analyses), Cedric-runs-it-directly for both ShareScope scripts, mandatory 12-month chart capture as Step 2b, Ron sub-agent spawn as Step 4, corrected tag format `[TICKER, Financial-Analysis, ShareScope-Research, Ron]`, Obsidian URI format `vault=<name>&file=<relative>` on Windows. Also added Appendix A with the mandatory report template Cedric passes Ron, and Appendix B with the v1.0/v2.0 changelog. Nina's legacy v1-report path is documented as retained-but-ignored -- a safety-net fallback in case Ron ever fails.

**Second: the Obsidian URI fix.** One-function edit to `_make_obsidian_uri()` in `sharescope_nlm_researcher.py` (around line 130). Old code emitted `obsidian://open?path=<url-encoded-absolute-path>` which doesn't click reliably on Windows. New code emits `obsidian://open?vault=Dex-MickP&file=<url-encoded-relative-path>` using `Path.relative_to()` and `Path.as_posix()` to build the relative portion; falls back to the old `path=` form if the report is somehow outside the vault root. The fix was proven live later in the session when the ENQ NLM researcher printed a fully-clickable link at end-of-run. SOP was updated in place to remove the "Fix pending" wording.

**Third: the Ron auth quirk investigation, which turned out to be a misdiagnosis.** Cedric ran a first sanity check -- `notebooklm ask` from the parent context -- and it returned "Authentication expired or invalid. Redirected to accounts.google.com...". The auth was NOT working in the parent context either. So yesterday's assumption that Ron had a special sub-agent inheritance problem was wrong: what Ron reported yesterday was the same real auth-expiry that persists today. Deeper dive: `notebooklm auth check` (offline) passed all five checks (storage exists, JSON valid, 28 cookies present, SID cookie present, structural integrity fine). `notebooklm auth check --test` (live) FAILED at Token fetch with the same expired message. `notebooklm auth refresh` (the non-interactive path) also failed. The finding: the cookies are physically present and don't reach their theoretical expiry for another ~316 days, but Google has invalidated the SESSION on their side (session rotation, security policy, or similar). The pipeline's `notebooklm_auth_status.json` cache measures cookie EXPIRY, not cookie VALIDITY, and the auth monitor only does a real live check every 6 hours -- so the cache can lie for up to 6 hours after Google invalidates. Mick then ran `notebooklm login` himself in a browser and Cedric re-ran `--test` which then passed cleanly. SOP was updated to document this properly (removed the "sub-agent inheritance" language, added the cache-lies-if-Google-invalidates note, added the "Cedric can add a live-canary pre-flight" as a future fix).

**Fourth: Ron as a permanent named agent.** Cedric investigated the existing agent-definition format by reading `Mick's-Writing-System\.claude\agents\article-writer.md` (5.9KB, YAML frontmatter with name/description/model, then markdown body of identity + instructions). Both `Mick's-Writing-System\.claude\agents\` and `Mick's-Dex-2nd-Brain\Dex-MickP\.claude\agents\` already contained the same three shared agents (article-writer, newsletter-writer, researcher-agent). Cedric wrote a 207-line `ron.md` containing Ron's full identity, the notebook + chart access instructions, the mandatory report template, market conventions (UK vs US), hard rules (UK English, ASCII only, sign off as Ron), the auth fallback, and "what you return / what you don't do" sections. Same file placed in BOTH vaults so Ron is available whenever Cedric runs from either Mick's-Writing-System (current session) or Mick's-Dex-2nd-Brain\Dex-MickP. A system-reminder confirmed shortly after that Ron was now available as an agent type WITHOUT a Claude Code restart -- the harness picked him up live. SOP updated to remove the "planned upgrade" language and replace with the current invocation pattern.

**Finally: the ENQ end-to-end validation.** With auth freshly re-authenticated and Ron now a named agent, Cedric ran a full clean pipeline pass on ENQ (Enquest) as the second-ticker proof after JSE yesterday. Step 2a: fresh CSVs from ShareScope (`sharescope_orchestrator.py ENQ`, 18.37s, 6 CSVs). Step 2b: 12-month chart PNG (`sharescope_chart_orchestrator.py ENQ`, 18.07s, went straight through the corrected "Other -> 1 year" selector path, chart confirmed showing 21/7/25 to Jul 26 with price marker at 26.625p +1.82% intraday). Step 3: NLM researcher in background (`sharescope_nlm_researcher.py ENQ "Enquest"`, ~5 minutes, 6 CSVs uploaded with 0 errors, 10 news sources imported with 0 warnings, notebook title auto-updated to `ENQ - Enquest_Updated_2026.07.21`, script printed a CORRECTLY-formed Obsidian URI at end of run confirming the URI fix). Step 4: Ron spawned via `Agent(subagent_type: "ron", prompt: <7-line per-run detail block>)` -- contrast with yesterday's ~60-line inline prompt via general-purpose. Ron ran for 9 minutes, used only 10 tool uses (vs 26 yesterday), and returned a comprehensive markdown report with proper multi-indicator TA drawn from the chart directly. Step 5: Cedric cleaned Ron's output (`&amp;` HTML entities to `&`, one `£` to `GBP` per Mick's ASCII rule), added the standard v2 frontmatter (analyst: Ron, chart_source recorded, tags including ENQ and Ron), saved as `_v2.md` in the ENQ Research folder. Delivered the working Obsidian link. Every fix from today and yesterday now proven live in a single clean run.

## Decisions made

- **SOP v2.0 is the current process reference** for the ShareScope + NBLM + Ron pipeline. It supersedes v1.0 in place at `C:\Vaults\Cowork\ShareScope-Project-Setup\3-SKILL-sharescope-nlm-research.md`. Appendix B carries the changelog.
- **The "Ron sub-agent NBLM CLI auth quirk" was a misdiagnosis.** Real cause is genuine Google session invalidation which the `notebooklm_auth_status.json` cache cannot see. The auth monitor's live check runs only every 6 hours, so the cache can lie for that long. When it happens, the fix is `notebooklm login` (browser-based, needs Mick).
- **Ron is now a permanent named agent** available as `subagent_type: "ron"` from any Claude Code session started in either the Mick's-Writing-System or Mick's-Dex-2nd-Brain\Dex-MickP vault. His full identity, template, and rules live in the definition file. Cedric passes only per-run details (ticker, company, notebook ID, chart path, market, date) on each spawn.
- **`_make_obsidian_uri()` in `sharescope_nlm_researcher.py` is fixed and verified.** All future pipeline-generated Obsidian links use the working `vault=Dex-MickP&file=<relative>` format.
- **ENQ v2 (Ron-authored, dated 2026.07.21) supersedes both ENQ v1 versions** (yesterday's tag-fixed Nina-authored 2026.07.20 v1, and today's Nina-authored 2026.07.21 v1 legacy safety-net). All three retained in the ENQ Research folder; v2 is the current definitive analysis.
- **The pipeline now works exactly as the SOP describes.** Yesterday's decisions have all been shipped and validated. Any new-ticker `/research [TICKER]` from tomorrow onward should follow the SOP path cleanly.

## Files changed this thread

- [MODIFY] `sharescope_nlm_researcher.py` `_make_obsidian_uri()` (around line 130) -- rewrote to emit `vault=Dex-MickP&file=<relative>` format with a fallback to `path=` if the report is somehow outside the vault root. Verified working in the ENQ end-of-run.
- [REWRITE] `C:\Vaults\Cowork\ShareScope-Project-Setup\3-SKILL-sharescope-nlm-research.md` -- SOP v1.0 -> v2.0. Approximately 330 lines. Includes new architecture section, revised steps 2/2b/3/4/5, updated troubleshooting table with tag/chart/auth entries, plus Appendix A (mandatory report template) and Appendix B (changelog).
- [CREATE] `C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\.claude\agents\ron.md` -- 207-line Ron agent definition. Full identity, notebook + chart access instructions, mandatory report template, market conventions (UK/US), hard rules (UK English + ASCII), auth fallback, output contract.
- [CREATE] `C:\Vaults\Mick's-Writing-System\.claude\agents\ron.md` -- identical copy for cross-vault availability.
- [CREATE] `C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\04-Projects\2026.04.04-ShareScope-Automation\downloads\ENQ\2026.07.21-14_14_*.csv` -- fresh 6-CSV set for today's ENQ run.
- [CREATE] `C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\04-Projects\2026.04.04-ShareScope-Automation\downloads\ENQ\2026.07.21-14_15_ENQ_chart_12m.png` -- corrected 12-month ENQ chart PNG (used by Ron for TA).
- [CREATE] `06-Resources\Research-Log\Research\ENQ\2026.07.21 - ENQ - Enquest - AI - Financial Analysis.md` (v1, Nina's legacy safety-net) -- auto-generated by the researcher script; not used, retained per SOP.
- [CREATE] `06-Resources\Research-Log\Research\ENQ\2026.07.21 - ENQ - Enquest - AI - Financial Analysis_v2.md` (Ron-authored, definitive) -- the real ENQ analysis. Includes full multi-indicator TA (support at 25/22/20/18p; resistance at 27/28p; MA stack read; OBV/RSI/MACD/AccDist/ADX all called out). BUY (special situation, driven by Malaysia PSC transformation and Magnus balance-sheet fix).
- [UPDATE] `_index.md` -- ENQ profile last_researched bumped to 2026.07.21, research_count incremented.

## Open questions / unfinished

- **Auth-canary live check in pre-flight (Option 2 offered today, not actioned).** The pipeline's `notebooklm_auth_status.json` cache measures cookie EXPIRY not VALIDITY and can lie for up to 6 hours after Google invalidates a session. Fix would be a small helper (in `sharescope_nlm_researcher.py` or a new `nlm_preflight.py`) that runs `notebooklm auth check --test` before Step 3 and either proceeds or asks Mick to `notebooklm login`. Roughly 15 minutes work. Would prevent the "cache lied, run stalled at NLM upload" scenario entirely. Cedric offered this today; Mick chose the ENQ end-to-end validation instead. Ready to slot in next session.
- **Nina's legacy v1 report is still auto-generated by the researcher script.** Every ENQ or JSE run today produces a Nina-authored v1 in the vault (from `run_financial_analysis()` in `sharescope_nlm_researcher.py`) plus Ron's authoritative v2. SOP says Cedric IGNORES v1 for the current flow, and the v1 is documented as a retained safety-net. If Mick wants the code trimmed so the v1 stops being generated at all, that's a one-line change: comment out the call to `run_financial_analysis()` in the pipeline's Step 3. Not urgent; the v1 files are labelled clearly and serve as a fallback if Ron ever fails.
- **Voice-activated pipeline path still calls the OLD Nina-only flow.** The file watcher / voice listener path at `sharescope_watcher.py` fires the legacy pipeline. It doesn't yet know about chart capture or Ron. To keep parity with the interactive path, that watcher needs updating too. Not urgent -- Mick can trigger via Cedric for the Ron-authored version.
- **ShareScope logout has a bug.** The orchestrator and chart orchestrator both log a warning at logout time: `Logout failed: Page.press() missing 1 required positional argument: 'key'`. This is a broken keyboard fallback in `sharescope_logout.py` when no logout button is found by any of the six candidate selectors. Cosmetic (the browser still gets closed cleanly on the next line) and non-blocking, but should be fixed -- currently every single run reports this "warning" and clutters the logs.
- **Notion Research Database log for JSE and ENQ never written.** Both today and yesterday the SOP's Step 6 wrap-up question ("Want me to log a Notion research entry?") was not actioned. Notion connector is authenticated. Trivial to do; needs a decision on title convention -- SOP currently says `YYYY.MM.DD - [Company Name] ([TICKER]): Ron's Analysis` for Ron-authored reports (up from `Cedric's Analysis` in the v1 SOP). Two entries to backfill if Mick wants.
- **Three broken JSE chart PNGs still in downloads/JSE/** from yesterday's chart-period debug loop (16:43, 16:55, 16:58 timestamps -- all show 8 years). Plus one broken JSE test PNG uploaded to the JSE NBLM notebook as source ID `2a9c622c-7f55-42b4-836f-6b4a484b26ce`, taking a slot (46% -> ~48% capacity). Both harmless but crufty. The next JSE pipeline run will selectively clear the [PIPE]-tagged source from the notebook automatically; the local PNG cruft needs a manual tidy.
- **Ron's forecast adjustments in ENQ v2 are his own numbers.** He took consensus down modestly (turnover -4.3%, PBT -5.8%, FCF -16.7%) and flipped EPS positive vs consensus's -0.84c on the grounds that consensus embeds the EPL deferred-tax hit while management's adjusted underlying figure is ~6.7c per share. These are analyst opinions, not Nina-derived facts, and are labelled as such in the report. If Mick disagrees with the framing the report needs a re-run or a manual edit.
- **Enquest currently has THREE report files for two consecutive days** in the ENQ Research folder (2026.07.20 v1 tag-fixed Nina, 2026.07.21 v1 Nina safety-net, 2026.07.21 v2 Ron-authored definitive). Not a problem, just noting for tidy-up decisions -- the SOP's expected steady state is one Ron v2 per research date per ticker, plus a Nina v1 that gets ignored.

## Next-thread pickup

- **Opener phrase:** "Cedric, resume from LATEST handover" or "Cedric, let's continue the ShareScope work."
- **First actions to consider, in priority order:**
  1. Add the auth-canary live-check to the pipeline pre-flight (Option 2 that Mick deferred today). Small, contained, prevents recurrence of the cache-lied scenario.
  2. Trim `sharescope_nlm_researcher.py` to skip Nina's legacy `run_financial_analysis()` call -- one commented-out line -- so v1 files stop being generated. Only worth doing if Mick wants the v1 fallback removed.
  3. Fix the cosmetic logout bug in `sharescope_logout.py` (`Page.press()` missing key argument). Every run currently logs a "warning" that isn't actually a warning.
  4. Update the voice-activated / watcher path (`sharescope_watcher.py`) to call the new Ron-based pipeline instead of the legacy Nina-only one.
  5. Backfill Notion Research Database entries for the three reports generated across yesterday and today.
- **What NOT to re-derive:** Ron pattern, chart period fix, tag format, Obsidian URI format, "sub-agent auth quirk" (was a misdiagnosis -- it's just genuine Google session invalidation). All settled and documented in the SOP.
- **If Mick opens with `/research [new ticker]`:** just follow SOP v2.0 -- everything's in place. Pre-flight, Cedric-runs-both-scripts, spawn Ron via `subagent_type: "ron"` with a 7-line prompt, save v2. That IS the pipeline now.
- **If Mick opens with something ENQ-related:** the Ron-authored v2 dated 2026.07.21 is the definitive report. Both Nina v1s (yesterday's tag-fixed, today's legacy safety-net) are retained but superseded.
- **If Mick opens with something JSE-related:** yesterday's Ron-authored v2 dated 2026.07.20 is the definitive report.

## Content Studio logged

None this thread -- this was research-pipeline infrastructure work, not Meet Cedric or other Content Studio material. Today's story arc (SOP codification -> "wait the auth quirk was a misdiagnosis" -> Ron becomes a first-class agent -> end-to-end validation on ENQ proves it all) would make a solid Meet Cedric episode if Mick wants to log it later: the theme is "when you build a permanent named agent, you stop re-explaining who they are on every invocation, and the tool count drops from 26 to 10". Yesterday's Ron-restoration piece and today's Ron-becomes-permanent piece could pair as a two-episode arc on how the pipeline architecture matured.
