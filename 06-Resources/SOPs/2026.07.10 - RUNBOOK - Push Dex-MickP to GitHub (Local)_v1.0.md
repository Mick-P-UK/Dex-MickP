---
title: RUNBOOK - Push Dex-MickP to GitHub (run locally on Mick's PC)
date: 2026-07-10
status: active - runbook
author: Cedric (PAIDA)
generated-by: AI
why: Dex-MickP was found 5 commits ahead of GitHub (nothing pushed since ~2 Jul).
     Pushing needs local credentials, so it cannot be done from a cloud Cowork session.
---

# RUNBOOK - Push Dex-MickP to GitHub (Local)

WHERE TO RUN THIS: on Mick's PC, in Claude Code (recommended) or a PowerShell
window. NOT from a cloud Cowork chat - the cloud sandbox has no GitHub
credentials, so it cannot push.

Repo:    C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP
Remote:  origin  ->  https://github.com/Mick-P-UK/Dex-MickP
Branch:  main
State at 2026-07-10 evening: 5 local commits NOT yet on GitHub, plus 1 new
untracked file (today's verification note) and a large number of other
modified files (left to the daily auto-commit, see Part 4).

------------------------------------------------------------------------
## EASIEST WAY - hand it to Claude Code (plain English)

Open Claude Code on your PC in the Dex-MickP folder and paste this:

  "Please back up my Dex-MickP repo to GitHub. It is at
   C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP, remote origin
   https://github.com/Mick-P-UK/Dex-MickP, branch main, currently 5 commits
   ahead of origin. Step 1: run git status -sb and git push origin main to
   clear the backlog. Step 2: add and commit just this one new file
   '06-Resources/2026.07.10 - Cloud Cowork Impact Verification - ShareScope,
   NotebookLM, ax-trees.md' with a sensible message, then push. If the push
   fails on authentication, show me the exact error and STOP - do not try to
   store or hardcode any credentials."

Claude Code can handle an auth prompt interactively, which is why this is the
easiest route.

------------------------------------------------------------------------
## MANUAL WAY - PowerShell commands (if you prefer to do it yourself)

Note: the vault path contains an apostrophe, so it MUST be in double quotes.

### Part 1 - go to the repo and look before you leap
  cd "C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP"
  git status -sb
WHAT GOOD LOOKS LIKE: the first line shows
  ## main...origin/main [ahead 5]
That confirms 5 commits waiting to go up.

### Part 2 - push the backlog
  git push origin main
WHAT GOOD LOOKS LIKE: a few lines ending with something like
  <old>..<new>  main -> main
Then re-run:
  git status -sb
and the first line should now say just "## main...origin/main" with NO
"[ahead 5]". That means GitHub is caught up.

### Part 3 - commit and push today's verification note
  git add "06-Resources/2026.07.10 - Cloud Cowork Impact Verification - ShareScope, NotebookLM, ax-trees.md"
  git commit -m "Add cloud-Cowork impact verification note (2026-07-10)"
  git push origin main
WHAT GOOD LOOKS LIKE: one file changed in the commit summary, then a clean push.

------------------------------------------------------------------------
## IF THE PUSH FAILS ON AUTHENTICATION

This is the most likely snag (it is probably why the auto-push stalled). If you
see "Authentication failed", "could not read Username", or a credential prompt:

Option 1 (easiest): open GitHub Desktop if you have it. It will show the
unpushed commits for Dex-MickP - click "Push origin". It manages the login for
you.

Option 2: in Claude Code, say "the git push failed authentication - help me
sign in to GitHub for this repo". It can walk you through gh auth login or a
Personal Access Token.

Do NOT paste a token into a cloud Cowork chat, and do NOT hardcode it anywhere.
Credentials stay on your PC (Windows Credential Manager / GitHub Desktop).

------------------------------------------------------------------------
## PART 4 - the large number of other modified files (do NOT bulk-commit blindly)

git status will also show many other modified files. These are normally handled
by the daily auto-commit job (the "Daily commit: <date>" entries in the log).
Do not sweep them all into one manual commit without a reason - that is the
worker's job and a blind bulk commit can bury a real change.

The thing worth checking once auth is fixed: the daily commits from 3-8 July are
the 5 that were stuck. If they push cleanly now, the auto-push was simply blocked
on auth and should resume by itself. If daily commits keep failing to reach
GitHub afterwards, that is a separate fault to diagnose (auth token expiry, or
the push step missing from the daily job).

------------------------------------------------------------------------
## STILL OUTSIDE ANY BACKUP (flagged 2026-07-10, decision deferred)

These are NOT in a git repo, so this runbook does not cover them. Decide later
whether to bring them under version control:
- C:\Vaults\Cowork\CLAUDE.md and C:\Vaults\Cowork\Researcher-Ron\CLAUDE.md
  (today's NotebookLM runtime fixes) - the Cowork folder root is not a git repo.
- C:\...\PAIDA Master - Second Brain\04-Projects\2026.05.09 - MCSB\ (today's two
  MCSB notes) - that folder is not a git repo.
- Dex-MickP\04-Projects\ is git-ignored by design, so the Meet Cedric episode
  there is not tracked either.

---
Provenance: C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\06-Resources\SOPs\2026.07.10 - RUNBOOK - Push Dex-MickP to GitHub (Local)_v1.0.md | Created 2026.07.10
