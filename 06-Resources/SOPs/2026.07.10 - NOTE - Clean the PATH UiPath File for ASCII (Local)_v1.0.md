---
title: NOTE - Clean the PATH UiPath analysis file for ASCII (run locally)
date: 2026-07-10
status: active - one-off task
author: Cedric (PAIDA)
generated-by: AI
why: This file was held back from the 2026-07-10 GitHub commit because the
     Dex-MickP pre-commit hook blocks non-ASCII characters. Clean it, then
     commit and push locally.
---

# NOTE - Clean the PATH UiPath File for ASCII (Local)

WHERE TO RUN: on Mick's PC, in Claude Code (recommended) or PowerShell. Not a
cloud Cowork session (the cloud mount is a stale snapshot and cannot see the
true bytes, and cannot push).

## The file
C:\Users\pavey... ->
C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\06-Resources\Research-Log\Research\PATH\2026.07.08 - PATH - UiPath Inc. - AI - Financial Analysis.md

## What is wrong
The pre-commit hook reported bytes 0x80 and 0x94 around lines 173-174. Those are
the tail bytes of UTF-8 "smart" punctuation - almost certainly an em dash
(U+2014) and curly double quotes (U+201C / U+201D), plus possibly curly
apostrophes (U+2019). They must be swapped for plain ASCII so the file reads as
straight quotes and hyphens. Nothing about the meaning changes.

## EASIEST WAY - hand it to Claude Code (plain English)

Open Claude Code on the PC in the Dex-MickP folder and paste:

  "Please make this file ASCII-only without changing its meaning:
   '06-Resources/Research-Log/Research/PATH/2026.07.08 - PATH - UiPath Inc. -
   AI - Financial Analysis.md'. Replace curly single quotes with ', curly
   double quotes with \", en/em dashes with -, ellipsis with ..., a euro sign
   with EUR, and strip any other non-ASCII byte. Detect the file's actual
   encoding first (it may be UTF-8 or Windows-1252) so you decode it correctly,
   then save it as UTF-8 without BOM. Afterwards run
   grep -P '[\\x80-\\xFF]' on the file to prove zero non-ASCII bytes remain,
   then git add just that file, commit it with a sensible message, and push."

Claude Code can iterate and re-check the hook, which is why this is the safest
route.

## MANUAL WAY - PowerShell (fallback)

Try this first (treats the file as UTF-8, which is the likely case):

  $f = "C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\06-Resources\Research-Log\Research\PATH\2026.07.08 - PATH - UiPath Inc. - AI - Financial Analysis.md"
  $t = [System.IO.File]::ReadAllText($f, [System.Text.Encoding]::UTF8)
  $t = $t -replace [char]0x2019,"'" -replace [char]0x2018,"'"
  $t = $t -replace [char]0x201C,'"' -replace [char]0x201D,'"'
  $t = $t -replace [char]0x2013,'-' -replace [char]0x2014,'-'
  $t = $t -replace [char]0x2026,'...' -replace [char]0x20AC,'EUR' -replace [char]0x00A0,' '
  $t = -join ($t.ToCharArray() | Where-Object { [int]$_ -lt 128 })
  [System.IO.File]::WriteAllText($f, $t, (New-Object System.Text.UTF8Encoding($false)))

If the text looks garbled after that (mojibake), the file was Windows-1252, not
UTF-8 - re-run with the read line changed to:
  $t = [System.IO.File]::ReadAllText($f, [System.Text.Encoding]::GetEncoding(1252))
and run the rest again.

## PROVE IT IS CLEAN, THEN COMMIT AND PUSH

  cd "C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP"
  git diff --stat -- "06-Resources/Research-Log/Research/PATH/2026.07.08 - PATH - UiPath Inc. - AI - Financial Analysis.md"
  git add "06-Resources/Research-Log/Research/PATH/2026.07.08 - PATH - UiPath Inc. - AI - Financial Analysis.md"
  git commit -m "Clean non-ASCII typographic chars in PATH UiPath analysis (ASCII-only rule)"
  git push origin main

WHAT GOOD LOOKS LIKE: the pre-commit line says the .md file(s) are clean, the
commit is created, and the push ends with <old>..<new>  main -> main. After
this, git status -sb should show no "[ahead]" - fully backed up.

## SANITY CHECK BEFORE COMMITTING
Skim the diff. The only changes should be quotes/dashes turning into straight
ASCII versions. If whole sentences changed, the encoding step was wrong - undo
with: git checkout -- "<the file>" and try the other encoding.

---
Provenance: C:\Vaults\Mick's-Dex-2nd-Brain\Dex-MickP\06-Resources\SOPs\2026.07.10 - NOTE - Clean the PATH UiPath File for ASCII (Local)_v1.0.md | Created 2026.07.10
